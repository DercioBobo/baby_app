// bebélog — API configuration
// In production this is the same domain, so no CORS issues.
// In local development, change apiUrl to point to your dev server.
const CONFIG = {
  apiUrl: '/api',   // relative — works on InfinityFree and any other host
};
